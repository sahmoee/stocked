// ServingSizeView.swift — NEUTRALIZED (Cook Now redesign, Direction B).
//
// This was the blocking "How many people are we feeding?" step shown before the
// old Cook Now hub. The redesigned Cook Now applies the saved household size
// silently as the session default (CookNowSession) and exposes a compact
// "Cooking for N" control on the dashboard instead, so this screen is no longer
// part of any flow. Kept as an empty stub because build tooling cannot remove
// files from the repo. If a future flow needs a dedicated serving picker,
// replace this stub.

import SwiftUI
