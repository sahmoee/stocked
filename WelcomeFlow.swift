// WelcomeFlow.swift — NEUTRALIZED (Build 300).
//
// This was a full-screen, swipeable five-page welcome carousel shown once on first launch. It
// has been removed in favor of the per-page coachmark tour (Coachmark.swift), which introduces
// each page's features in place, with a glow, the first time that page is visited. The old
// carousel duplicated that purpose, so it was retired.
//
// Kept as an empty stub rather than deleted because the build tooling cannot remove files from
// the repo.

import SwiftUI

// Intentionally empty. New-user introduction now lives in Coachmark.swift.
