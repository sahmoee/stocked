// SettingsView.swift — NEUTRALIZED (Build 293, redundancy cleanup #1).
//
// This was a parallel, standalone settings screen that the app no longer used. The live
// settings UI is SettingsContent (and DrawerContent) in DrawerSettingsContent.swift, which
// is what the drawer and the Preferences page present. SettingsView was only referenced by
// its own SwiftUI preview, so it was removed as dead code to stop the two settings
// implementations from drifting apart.
//
// Kept as an empty stub rather than deleted because the build tooling cannot remove files
// from the repo.

import SwiftUI

// Intentionally empty. The live settings UI lives in DrawerSettingsContent.swift.
