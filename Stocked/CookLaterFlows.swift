// CookLaterFlows.swift
// Cook Later is now one planning workspace. The existing type name remains as the
// stable navigation destination used by CookHubView and older deep links.

import SwiftUI

struct CookLaterHomeView: View {
    var body: some View {
        CookLaterWorkspaceView()
    }
}
