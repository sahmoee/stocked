// CoachWalkthrough.swift — removed.
//
// The first-run arrow tour ("shows you where things are") was replaced by WelcomeFlow.swift,
// a full-screen swipeable intro. This file is intentionally left empty so the old
// CoachWalkthrough type no longer exists. It can be deleted from the project entirely.

import SwiftUI
