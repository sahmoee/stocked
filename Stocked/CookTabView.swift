// CookTabView.swift — NEUTRALIZED (Cook Now redesign, Direction B).
//
// This view was an earlier Cook tab that has not been rendered since the Cook
// tab moved to CookHubView (see MainTabView.tabContent). It still referenced
// the now-neutralized ServingSizeView, so it is stubbed out with it. Kept as an
// empty stub because build tooling cannot remove files from the repo.

import SwiftUI
