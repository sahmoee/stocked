// MainHubView.swift — NEUTRALIZED (Cook Now redesign, Direction B).
//
// This was the legacy Foods · Moods · Surprise Me hub reached only through the
// (also neutralized) ServingSizeView step. The redesigned Cook Now dashboard in
// CookHubView.swift owns those pathways directly, and Surprise Me now rolls a
// pick inline via GuestDataStore.surpriseRecipeTuned(). Kept as an empty stub
// because build tooling cannot remove files from the repo.

import SwiftUI
