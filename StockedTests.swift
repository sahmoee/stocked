// StockedTests.swift
// Test code removed — this file intentionally contains no XCTest imports or tests.
// (Unit tests require a separate Unit Testing Bundle target; running them inside the
// main app target fails because XCTest isn't available there.)
