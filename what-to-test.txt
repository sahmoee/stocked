Stocked — What to Test (latest build)

REMOTE RECIPE FEED (new)
- With RemoteRecipeFeed.feedURLString set to a valid recipes.json url, pull to refresh
  Discover. Confirm the feed's recipes appear (grouped under their source, e.g. Community
  Recipes) and are searchable and openable.
- Edit the hosted recipes.json (add a recipe), wait past the cache window (or reinstall),
  refresh, and confirm the new recipe shows up WITHOUT an app update.
- With feedURLString empty, confirm Discover behaves exactly as before (feed no-ops).
- Offline: confirm previously fetched feed recipes still appear from cache.

REGRESSION
- Other Discover sources still load and de-duplicate correctly.
