Stocked — What to Test (latest build)

BRAND & PRICE (new)
- With RAPIDAPI_KEY set in Secrets.xcconfig, open an inventory item (tap a row to edit it).
  Under the item name, confirm a brand and a price appear once loaded (from Walmart/Amazon
  catalog), with the source label.
- Open the same item again and confirm it appears instantly (cached, no re-fetch, no flicker).
- With NO key set, confirm the item screen looks exactly as before (the badge simply hides,
  no blank space, no error).
- Try items with clear matches (milk, eggs, coffee) and odd/rare names (should just hide).

REGRESSION
- Editing an item (name, zone, quantity, expiry, photo) still saves correctly.
- Inventory rows, Add Item, and Scan Barcode still open first-tap.

Note: brand/price is currently shown on the item detail screen. Other screens (grocery rows,
add item, barcode result) can show the same badge with a one-line drop-in.
