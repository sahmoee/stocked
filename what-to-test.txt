Stocked — What to Test (latest build)

NEARBY STORES (main fix)
- Open the grocery store finder and allow location (or enter a zip).
- Confirm MULTIPLE nearby grocery stores now appear on the map and in the list, sorted by
  distance — not just a single store. Try it somewhere you know several stores exist.
- Enter a zip far from you and confirm it recenters and lists stores near that zip.

GROCERY PRODUCT / BRAND / PRICE LOOKUP (new, if RAPIDAPI_KEY is set)
- Where wired in, search a common item (milk, eggs, coffee) and confirm brand and price
  results come back, and that repeating the same search is instant (cached, no re-fetch).
- With no key set, the feature should simply show nothing (no crash, no error).

REGRESSION
- Grocery list still groups by store section as before.
- Preferred store and receipt store detection still work.

Note: aisle-level data is not provided by any available API; aisle grouping continues to
use Stocked's store-section logic.
