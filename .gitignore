# ── Secrets — NEVER commit real keys ──
Secrets.xcconfig

# ── Xcode ──
xcuserdata/
*.xcuserstate
*.xcscmblueprint
build/
DerivedData/
.DS_Store

# ── Cloudflare Worker local state ──
**/.wrangler/
node_modules/

# (Secrets.example.xcconfig IS committed — it's just the template, no real values.)

# BuildBuddy local undo points — never commit
.buildbuddy-backups/

# Cleanup staging (Claude can't delete on this mount; empty this folder in Finder)
_to_delete/

# macOS metadata
._*
**/._*
*.tgz
