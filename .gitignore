# ── Secrets — NEVER commit real keys ──
Secrets.xcconfig

# ── Xcode ──
xcuserdata/
*.xcuserstate
*.xcscmblueprint
build/
DerivedData/
.DS_Store

# ── Cloudflare Worker local state ──
**/.wrangler/
node_modules/

# (Secrets.example.xcconfig IS committed — it's just the template, no real values.)

# BuildBuddy local undo points — never commit
.buildbuddy-backups/

# Deliverable snapshots — regenerate from git, never keep source copies in-tree
*.zip

# macOS metadata
._*
**/._*
*.tgz

# Local Claude session settings
.claude/settings.local.json
