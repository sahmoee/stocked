// SpeechReader.swift — intentionally empty.
//
// The read-aloud helper lives in CookingFlow.swift (it predates this file; the
// batch-2 delivery accidentally created a duplicate declaration here). The
// CookingFlow copy was upgraded in place with the @Observable per-step API this
// file introduced. BuildBuddy applies overlay without deleting, so this stub
// neutralizes the duplicate; it can be removed from the project at any time.
