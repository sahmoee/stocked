// RecipeQuality.swift — NEUTRALIZED.
//
// This file originally added a second RecipeQuality scorer, which collided with the existing
// RecipeQuality enum in RecipeSupport.swift (used by RecipeDatabase). Recipe quality scoring
// already exists there, so this duplicate was removed. The one genuinely new piece — mapping a
// quality score to a SourceBadge — was added as an extension on the existing RecipeQuality in
// RecipeSupport.swift instead.
//
// Kept as an empty stub because the build tooling cannot delete files.

import Foundation

// Intentionally empty. See RecipeQuality in RecipeSupport.swift.
